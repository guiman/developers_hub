<table class="table">
  <thead>
    <tr>
      <th>Profile</th>
      <th>Location</th>
    </tr>
  </thead>
  <tbody>
    <% @candidates.each do |candidate| %>
      <tr>
        <td><a href="/user/<%= candidate.id %>"><%= candidate.name %></a></td>
        <td><%= candidate.location %></td>
      </tr>
    <% end %>
  </tbody>
</table>
